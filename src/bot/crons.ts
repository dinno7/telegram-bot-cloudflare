import { Bot } from 'grammy';

export async function injectCrobJobsInstructions(bot: Bot, controller: ScheduledController): Promise<void> {
	// await bot.api.sendMessage(process.env.ADMIN_ID, 'Hello, this msg send in cron job');
	// ...
}
